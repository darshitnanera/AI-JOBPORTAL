# Deployed backend origin (no trailing slash, no /api suffix).
# Both names are defined because the codebase reads each in different modules.
VITE_API_URL=https://ai-jobportal-backend.onrender.com
VITE_API_BASE_URL=https://ai-jobportal-backend.onrender.com
