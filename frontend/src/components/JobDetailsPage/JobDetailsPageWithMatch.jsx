/**
 * JobDetailsPageWithMatch
 *
 * The AI match score and skill-gap analysis are now rendered inline by
 * JobDetailsPage itself (header → match score → skill gap → description →
 * requirements → apply). This module stays as a named entry point for older
 * imports and simply renders that page.
 */
import JobDetailsPage from "./JobDetailsPage";

export default JobDetailsPage;
